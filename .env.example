# Site
NEXT_PUBLIC_SITE_URL=https://www.rollnbowl.be

# Admin (kitchen + inventory) — change in Production!
ADMIN_PIN=4355
# Optional: separate secret for signed admin session cookies (defaults to ADMIN_PIN)
# ADMIN_SESSION_SECRET=

# Stripe (online checkout)
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
# Stripe Dashboard → Webhooks → https://www.rollnbowl.be/api/stripe/webhook
# Event: payment_intent.succeeded
STRIPE_WEBHOOK_SECRET=whsec_...

# Vercel KV / Upstash (order inbox — phone → kitchen)
# KV_REST_API_URL=
# KV_REST_API_TOKEN=

# Lightspeed / POS (optional)
# LIGHTSPEED_ORDER_PUSH_URL=
# LIGHTSPEED_API_TOKEN=
